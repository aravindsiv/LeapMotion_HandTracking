#include "Leap.h"
#include "LeapRecorder.hpp"

class Record {
public:
	// int i = 0;
	// Record::Record() {};
	// Record::~Record() {};
	Record() {};
	~Record() {};
	// int Record::record(std::string filename)
	int record(std::string filename);

};

